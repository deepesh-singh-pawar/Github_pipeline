name: Hello

on: 
  push: 
   branches: [main]

jobs:

  greet:
   runs-on: ubuntu-latest
   steps:
     - name: say hello
       run: echo "Hi to first workflow"

     - name: say hello to username
       run: echo "Hello $USER"
